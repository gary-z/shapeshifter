diff --git a/src/solver/likelihood.rs b/src/solver/likelihood.rs
index 62ae9a1..f98c88f 100644
--- a/src/solver/likelihood.rs
+++ b/src/solver/likelihood.rs
@@ -230,6 +230,13 @@ pub(super) struct ReverseLikelihood {
 }
 
 impl ReverseLikelihood {
+    /// Bound the exponential state space of each 3x3 window independently of
+    /// the board dimensions or number of pieces.
+    pub(super) fn can_precompute(modulus: u8) -> bool {
+        const MAX_WINDOW_STATES: usize = 1 << 15;
+        usize::from(modulus).pow(9) <= MAX_WINDOW_STATES
+    }
+
     pub(super) fn precompute(
         placements: &[PiecePlacements],
         height: u8,
@@ -317,28 +324,31 @@ mod tests {
 
     #[test]
     fn incremental_placement_scores_match_full_scores() {
-        let board = Board::from_grid(
-            &[&[0, 1, 2, 0], &[1, 2, 0, 1], &[2, 0, 1, 2], &[0, 1, 2, 0]],
-            3,
-        );
-        let placements = vec![
-            Piece::from_grid(&[&[true, true]]).placements(4, 4),
-            Piece::from_grid(&[&[true], &[true]]).placements(4, 4),
-        ];
-        let likelihood = ReverseLikelihood::precompute(&placements, 4, 4, 3);
-        let indices = (0..placements[0].len())
-            .map(|index| index as u8)
-            .collect::<Vec<_>>();
-        let mut scores = [0i32; super::super::backtrack::MAX_PLACEMENTS];
-        likelihood.score_placements(&board, 0, &indices, &mut scores);
+        for modulus in 2..=3 {
+            let grid: Vec<Vec<u8>> = (0..4)
+                .map(|row| (0..4).map(|column| (row + column) % modulus).collect())
+                .collect();
+            let rows = grid.iter().map(Vec::as_slice).collect::<Vec<_>>();
+            let board = Board::from_grid(&rows, modulus);
+            let placements = vec![
+                Piece::from_grid(&[&[true, true]]).placements(4, 4),
+                Piece::from_grid(&[&[true], &[true]]).placements(4, 4),
+            ];
+            let likelihood = ReverseLikelihood::precompute(&placements, 4, 4, modulus);
+            let indices = (0..placements[0].len())
+                .map(|index| index as u8)
+                .collect::<Vec<_>>();
+            let mut scores = [0i32; super::super::backtrack::MAX_PLACEMENTS];
+            likelihood.score_placements(&board, 0, &indices, &mut scores);
 
-        for &placement_index in &indices {
-            let mut child = board;
-            child.apply_piece(placements[0][placement_index as usize].2);
-            assert_eq!(
-                scores[placement_index as usize],
-                likelihood.board_score(&child, 1)
-            );
+            for &placement_index in &indices {
+                let mut child = board;
+                child.apply_piece(placements[0][placement_index as usize].2);
+                assert_eq!(
+                    scores[placement_index as usize],
+                    likelihood.board_score(&child, 1)
+                );
+            }
         }
     }
 }
diff --git a/src/solver/mod.rs b/src/solver/mod.rs
index 497bfc9..f970f72 100644
--- a/src/solver/mod.rs
+++ b/src/solver/mod.rs
@@ -61,14 +61,7 @@ pub struct PreparedSearch {
 
 #[cfg(not(target_arch = "wasm32"))]
 fn should_use_guided_frontier(game: &Game, parallel: bool, exhaustive: bool) -> bool {
-    let board = game.board();
-    // The tables are exponential in M and the large frontier only pays for
-    // itself at the current M=3, 10x11, 22-piece search boundary.
-    parallel
-        && !exhaustive
-        && board.m() == 3
-        && usize::from(board.height()) * usize::from(board.width()) == 110
-        && game.pieces().len() >= 22
+    parallel && !exhaustive && likelihood::ReverseLikelihood::can_precompute(game.board().m())
 }
 
 struct SolverData {
@@ -108,20 +101,9 @@ pub fn prepare(game: &Game, parallel: bool, exhaustive: bool) -> PreparedSearch
 
     let (board, piece_order, data) = prepare_search(game, guided_frontier);
     #[cfg(not(target_arch = "wasm32"))]
-    let adaptive = (parallel
-        && !exhaustive
-        && board.m() == 3
-        && usize::from(board.height()) * usize::from(board.width()) == 56)
-        .then(|| adaptive::AdaptiveSearch::precompute(game));
+    let adaptive = (parallel && !exhaustive).then(|| adaptive::AdaptiveSearch::precompute(game));
     #[cfg(not(target_arch = "wasm32"))]
-    let regional = (parallel
-        && !exhaustive
-        && match board.m() {
-            3 => usize::from(board.height()) * usize::from(board.width()) >= 100,
-            4 => usize::from(board.height()) * usize::from(board.width()) >= 64,
-            _ => false,
-        })
-    .then(|| regional::RegionalSearch::precompute(game));
+    let regional = (parallel && !exhaustive).then(|| regional::RegionalSearch::precompute(game));
     PreparedSearch {
         board,
         piece_order,
@@ -197,8 +179,8 @@ impl PreparedSearch {
         #[cfg(not(target_arch = "wasm32"))]
         if let Some(adaptive) = adaptive {
             let workers = std::thread::available_parallelism().map_or(1, usize::from);
-            eprintln!("adaptive search: {workers} workers, 30s budget");
-            let (solution, nodes) = adaptive.solve(std::time::Duration::from_secs(30), workers);
+            eprintln!("adaptive search: {workers} workers, 25s budget");
+            let (solution, nodes) = adaptive.solve(std::time::Duration::from_secs(25), workers);
             total_nodes += nodes;
             if solution.is_some() {
                 return SolveResult {
@@ -211,8 +193,8 @@ impl PreparedSearch {
         #[cfg(not(target_arch = "wasm32"))]
         if let Some(regional) = regional {
             let workers = std::thread::available_parallelism().map_or(1, usize::from);
-            eprintln!("regional search: {workers} workers, 30s budget");
-            let (solution, nodes) = regional.solve(std::time::Duration::from_secs(30), workers);
+            eprintln!("regional search: {workers} workers, 25s budget");
+            let (solution, nodes) = regional.solve(std::time::Duration::from_secs(25), workers);
             total_nodes += nodes;
             if solution.is_some() {
                 return SolveResult {
@@ -246,7 +228,11 @@ impl PreparedSearch {
                                 data,
                                 exhaustive,
                                 is_guided_attempt,
-                                None,
+                                // Leave time for root backtracking when the
+                                // ranked prefix does not lead to a solution.
+                                is_guided_attempt.then(|| {
+                                    std::time::Instant::now() + std::time::Duration::from_secs(30)
+                                }),
                             )
                         }
                         #[cfg(target_arch = "wasm32")]
@@ -453,19 +439,21 @@ mod tests {
 
         let mut rng = <rand::rngs::SmallRng as rand::SeedableRng>::seed_from_u64(50_101);
         let game = crate::generate::generate_for_level(50, &mut rng).unwrap();
-        let (board, piece_order, data) = prepare_search(&game, false);
-        let start = Instant::now();
-        let result = parallel::solve_parallel::<3>(
-            &board,
-            &piece_order,
-            &data,
-            false,
-            false,
-            Some(start + Duration::from_millis(10)),
-        );
-        assert!(start.elapsed() < Duration::from_secs(5));
-        if let Some(solution) = result.solution {
-            verify_solution(&game, &solution);
+        let (board, piece_order, data) = prepare_search(&game, true);
+        for guided in [false, true] {
+            let start = Instant::now();
+            let result = parallel::solve_parallel::<3>(
+                &board,
+                &piece_order,
+                &data,
+                false,
+                guided,
+                Some(start + Duration::from_millis(10)),
+            );
+            assert!(start.elapsed() < Duration::from_secs(5));
+            if let Some(solution) = result.solution {
+                verify_solution(&game, &solution);
+            }
         }
     }
 
diff --git a/src/solver/parallel.rs b/src/solver/parallel.rs
index 3cc0f8a..66b74d2 100644
--- a/src/solver/parallel.rs
+++ b/src/solver/parallel.rs
@@ -6,6 +6,8 @@ use std::collections::VecDeque;
 use std::sync::atomic::{AtomicBool, AtomicU64, AtomicUsize, Ordering};
 use std::sync::{Condvar, Mutex};
 
+use rayon::prelude::*;
+
 use crate::core::board::Board;
 
 use super::backtrack::{
@@ -38,27 +40,29 @@ struct FrontierState {
     placement_indices: [u8; MAX_PIECES],
 }
 
-struct ScoredFrontierState {
+/// Keep only a placement descriptor while ranking a layer. Materialize boards
+/// for the globally retained candidates after merging the workers' best lists.
+struct FrontierCandidate {
     score: i32,
     serial: u64,
-    state: FrontierState,
+    placement_index: u8,
 }
 
-impl PartialEq for ScoredFrontierState {
+impl PartialEq for FrontierCandidate {
     fn eq(&self, other: &Self) -> bool {
         (self.score, self.serial) == (other.score, other.serial)
     }
 }
 
-impl Eq for ScoredFrontierState {}
+impl Eq for FrontierCandidate {}
 
-impl PartialOrd for ScoredFrontierState {
+impl PartialOrd for FrontierCandidate {
     fn partial_cmp(&self, other: &Self) -> Option<std::cmp::Ordering> {
         Some(self.cmp(other))
     }
 }
 
-impl Ord for ScoredFrontierState {
+impl Ord for FrontierCandidate {
     fn cmp(&self, other: &Self) -> std::cmp::Ordering {
         (self.score, self.serial).cmp(&(other.score, other.serial))
     }
@@ -67,15 +71,152 @@ impl Ord for ScoredFrontierState {
 const GUIDED_FRONTIER_WIDTH: usize = 200_000;
 const GUIDED_FRONTIER_DEPTH: usize = 8;
 
-fn likelihood_frontier<const MODULUS: usize>(
-    board: &Board,
+fn expand_frontier<const MODULUS: usize>(
+    states: &[FrontierState],
     data: &SolverData,
-) -> (Vec<SearchTask>, u64) {
-    eprintln!("building region-guided frontier...");
+    piece_index: usize,
+    width: usize,
+    deadline: Option<std::time::Instant>,
+) -> (Vec<FrontierState>, u64) {
     let likelihood = data
         .reverse_likelihood
         .as_ref()
         .expect("guided frontier requires likelihood precomputation");
+    let placements = &data.placements[piece_index];
+    let chunk_size = states.len().div_ceil(rayon::current_num_threads());
+    let batches: Vec<_> = states
+        .par_chunks(chunk_size)
+        .enumerate()
+        .map(|(chunk_index, chunk)| {
+            let mut best = BinaryHeap::with_capacity(width.min(chunk.len() * MAX_PLACEMENTS) + 1);
+            let mut nodes = 0u64;
+            for (state_offset, state) in chunk.iter().enumerate() {
+                if deadline.is_some_and(|limit| std::time::Instant::now() >= limit) {
+                    break;
+                }
+                let max_zero_cells =
+                    max_zero_cells_allowed::<MODULUS>(&state.position.board, data, piece_index);
+                let mut ranked_indices = [0u8; MAX_PLACEMENTS];
+                let ranked_count = if max_zero_cells <= 2 {
+                    rank_low_zero_hit_placements(
+                        &state.position.board,
+                        data.modulus,
+                        placements,
+                        &data.anchor_placements[piece_index],
+                        max_zero_cells,
+                        &mut ranked_indices,
+                    )
+                } else {
+                    rank_placements(
+                        &state.position.board,
+                        data.modulus,
+                        placements,
+                        max_zero_cells,
+                        &mut ranked_indices,
+                    )
+                };
+                let mut scores = [0i32; MAX_PLACEMENTS];
+                likelihood.score_placements(
+                    &state.position.board,
+                    piece_index,
+                    &ranked_indices[..ranked_count],
+                    &mut scores,
+                );
+
+                for (rank, &placement_index) in ranked_indices[..ranked_count].iter().enumerate() {
+                    let placement_index = placement_index as usize;
+                    if !is_canonical_placement_pair(
+                        data,
+                        piece_index,
+                        placement_index,
+                        state.position.previous_placement,
+                    ) {
+                        continue;
+                    }
+
+                    nodes += 1;
+                    let (_, _, mask) = placements[placement_index];
+                    let mut child_board = state.position.board;
+                    child_board.apply_piece(mask);
+                    let mut child_hits = state.position.hits;
+                    child_hits.apply_piece(mask);
+                    let next_piece_index = piece_index + 1;
+                    if data
+                        .monte_carlo
+                        .exceeds_hit_threshold(&child_hits, next_piece_index)
+                        || (next_piece_index < data.placements.len()
+                            && !state_is_feasible::<MODULUS>(&child_board, data, next_piece_index))
+                    {
+                        continue;
+                    }
+
+                    // Parent order and placement rank preserve the serial search's
+                    // tie order, independently of which worker finishes first.
+                    let parent_index = chunk_index * chunk_size + state_offset;
+                    let candidate = FrontierCandidate {
+                        score: scores[placement_index],
+                        serial: (parent_index * MAX_PLACEMENTS + rank) as u64,
+                        placement_index: placement_index as u8,
+                    };
+                    if best.len() == width && best.peek().is_some_and(|worst| candidate >= *worst) {
+                        continue;
+                    }
+                    best.push(candidate);
+                    if best.len() > width {
+                        best.pop();
+                    }
+                }
+            }
+            (best.into_vec(), nodes)
+        })
+        .collect();
+    let nodes = batches.iter().map(|(_, nodes)| nodes).sum();
+    if deadline.is_some_and(|limit| std::time::Instant::now() >= limit) {
+        return (Vec::new(), nodes);
+    }
+
+    // A global top-W candidate must be in its worker's top W. Merge all local
+    // lists before truncating, so no partition gets a fixed share of the beam.
+    let mut best: Vec<_> = batches
+        .into_iter()
+        .flat_map(|(entries, _)| entries)
+        .collect();
+    if best.len() > width {
+        best.select_nth_unstable(width);
+        best.truncate(width);
+    }
+    best.sort_unstable();
+    let next_states = best
+        .into_iter()
+        .map(|candidate| {
+            let parent = &states[candidate.serial as usize / MAX_PLACEMENTS];
+            let placement_index = candidate.placement_index as usize;
+            let mut position = parent.position;
+            let mask = placements[placement_index].2;
+            position.board.apply_piece(mask);
+            position.hits.apply_piece(mask);
+            position.piece_index = piece_index + 1;
+            position.previous_placement =
+                next_previous_placement(data, piece_index, placement_index);
+            let mut placement_indices = parent.placement_indices;
+            placement_indices[piece_index] = candidate.placement_index;
+            FrontierState {
+                position,
+                placement_indices,
+            }
+        })
+        .collect();
+    (next_states, nodes)
+}
+
+fn likelihood_frontier<const MODULUS: usize>(
+    board: &Board,
+    data: &SolverData,
+    width: usize,
+    depth: usize,
+    deadline: Option<std::time::Instant>,
+) -> (Vec<SearchTask>, u64) {
+    eprintln!("building region-guided frontier...");
     let mut states = vec![FrontierState {
         position: SearchPosition {
             board: *board,
@@ -87,105 +228,11 @@ fn likelihood_frontier<const MODULUS: usize>(
     }];
     let mut nodes = 0u64;
 
-    for piece_index in 0..GUIDED_FRONTIER_DEPTH.min(data.placements.len()) {
-        let mut best = BinaryHeap::with_capacity(GUIDED_FRONTIER_WIDTH + 1);
-        let mut serial = 0u64;
-        for state in states {
-            let placements = &data.placements[piece_index];
-            let max_zero_cells =
-                max_zero_cells_allowed::<MODULUS>(&state.position.board, data, piece_index);
-            let mut ranked_indices = [0u8; MAX_PLACEMENTS];
-            let ranked_count = if max_zero_cells <= 2 {
-                rank_low_zero_hit_placements(
-                    &state.position.board,
-                    data.modulus,
-                    placements,
-                    &data.anchor_placements[piece_index],
-                    max_zero_cells,
-                    &mut ranked_indices,
-                )
-            } else {
-                rank_placements(
-                    &state.position.board,
-                    data.modulus,
-                    placements,
-                    max_zero_cells,
-                    &mut ranked_indices,
-                )
-            };
-            let mut scores = [0i32; MAX_PLACEMENTS];
-            likelihood.score_placements(
-                &state.position.board,
-                piece_index,
-                &ranked_indices[..ranked_count],
-                &mut scores,
-            );
-
-            for &placement_index in &ranked_indices[..ranked_count] {
-                let placement_index = placement_index as usize;
-                if !is_canonical_placement_pair(
-                    data,
-                    piece_index,
-                    placement_index,
-                    state.position.previous_placement,
-                ) {
-                    continue;
-                }
-
-                nodes += 1;
-                let (_, _, mask) = placements[placement_index];
-                let mut child_board = state.position.board;
-                child_board.apply_piece(mask);
-                let mut child_hits = state.position.hits;
-                child_hits.apply_piece(mask);
-                let next_piece_index = piece_index + 1;
-                if data
-                    .monte_carlo
-                    .exceeds_hit_threshold(&child_hits, next_piece_index)
-                    || (next_piece_index < data.placements.len()
-                        && !state_is_feasible::<MODULUS>(&child_board, data, next_piece_index))
-                {
-                    continue;
-                }
-
-                let score = scores[placement_index];
-                serial += 1;
-                if best.len() == GUIDED_FRONTIER_WIDTH
-                    && best.peek().is_some_and(|worst: &ScoredFrontierState| {
-                        (score, serial) >= (worst.score, worst.serial)
-                    })
-                {
-                    continue;
-                }
-                let mut placement_indices = state.placement_indices;
-                placement_indices[piece_index] = placement_index as u8;
-                best.push(ScoredFrontierState {
-                    score,
-                    serial,
-                    state: FrontierState {
-                        position: SearchPosition {
-                            board: child_board,
-                            hits: child_hits,
-                            piece_index: next_piece_index,
-                            previous_placement: next_previous_placement(
-                                data,
-                                piece_index,
-                                placement_index,
-                            ),
-                        },
-                        placement_indices,
-                    },
-                });
-                if best.len() > GUIDED_FRONTIER_WIDTH {
-                    best.pop();
-                }
-            }
-        }
-        states = best
-            .into_sorted_vec()
-            .into_iter()
-            .map(|entry| entry.state)
-            .collect();
+    for piece_index in 0..depth.min(data.placements.len()) {
+        let (next_states, layer_nodes) =
+            expand_frontier::<MODULUS>(&states, data, piece_index, width, deadline);
+        nodes += layer_nodes;
+        states = next_states;
         if states.is_empty() {
             break;
         }
@@ -594,7 +641,13 @@ pub(super) fn solve_parallel<const MODULUS: usize>(
 
     let work_queue = WorkQueue::new();
     let frontier_nodes = if guided_frontier {
-        let (tasks, nodes) = likelihood_frontier::<MODULUS>(board, data);
+        let (tasks, nodes) = likelihood_frontier::<MODULUS>(
+            board,
+            data,
+            GUIDED_FRONTIER_WIDTH,
+            GUIDED_FRONTIER_DEPTH,
+            deadline,
+        );
         work_queue.push_many(tasks);
         nodes
     } else {
@@ -610,6 +663,14 @@ pub(super) fn solve_parallel<const MODULUS: usize>(
         0
     };
 
+    if deadline.is_some_and(|limit| std::time::Instant::now() >= limit) {
+        return SolveResult {
+            solution: None,
+            nodes_visited: frontier_nodes,
+            progress: 0.0,
+        };
+    }
+
     let thread_count = std::thread::available_parallelism()
         .map(|parallelism| parallelism.get())
         .unwrap_or(4);
@@ -736,3 +797,60 @@ pub(super) fn solve_parallel<const MODULUS: usize>(
         progress: final_progress,
     }
 }
+
+#[cfg(test)]
+mod tests {
+    use super::*;
+    use crate::core::piece::Piece;
+    use crate::game::Game;
+
+    fn compare_frontiers<const MODULUS: usize>() {
+        let row: &[u8] = &[1; 4];
+        let board = Board::from_grid(&[row; 4], MODULUS as u8);
+        let single_cell = Piece::from_grid(&[&[true]]);
+        let game = Game::new(board, vec![single_cell; 16]);
+        let (board, _, data) = super::super::prepare_search(&game, true);
+        let serial = rayon::ThreadPoolBuilder::new()
+            .num_threads(1)
+            .build()
+            .unwrap();
+        let parallel = rayon::ThreadPoolBuilder::new()
+            .num_threads(4)
+            .build()
+            .unwrap();
+        for width in [1, 7, 32] {
+            let (expected, expected_nodes) =
+                serial.install(|| likelihood_frontier::<MODULUS>(&board, &data, width, 4, None));
+            let (actual, actual_nodes) =
+                parallel.install(|| likelihood_frontier::<MODULUS>(&board, &data, width, 4, None));
+            assert!(!expected.is_empty());
+            assert_eq!(expected_nodes, actual_nodes);
+            assert_eq!(expected.len(), actual.len());
+            for (expected, actual) in expected.iter().zip(actual) {
+                assert_eq!(expected.solution_prefix, actual.solution_prefix);
+                assert!(expected.position.board == actual.position.board);
+                let mut replay = board;
+                for (piece_index, &(row, column)) in expected.solution_prefix.iter().enumerate() {
+                    let mask = data.placements[piece_index]
+                        .iter()
+                        .find(|&&(r, c, _)| (r, c) == (row, column))
+                        .unwrap()
+                        .2;
+                    replay.apply_piece(mask);
+                }
+                assert!(replay == expected.position.board);
+                assert_eq!(expected.position.piece_index, actual.position.piece_index);
+                assert_eq!(
+                    expected.position.previous_placement,
+                    actual.position.previous_placement
+                );
+            }
+        }
+    }
+
+    #[test]
+    fn frontier_keeps_global_ranking_across_worker_counts() {
+        compare_frontiers::<2>();
+        compare_frontiers::<3>();
+    }
+}
diff --git a/src/solver/regional.rs b/src/solver/regional.rs
index 58b8a8c..ec1a991 100644
--- a/src/solver/regional.rs
+++ b/src/solver/regional.rs
@@ -203,9 +203,10 @@ impl RegionalSearch {
                     .collect()
             })
             .collect();
-        // Keep the joint distributions affordable as the modulus increases.
-        // Four-cell regions have 256 states at M=4; six cells would need 4096.
-        let region_shapes = if modulus == 3 {
+        // Choose region sizes by the cost of their joint distributions, using
+        // the same state budget for every board and modulus.
+        const MAX_REGION_STATES: usize = 1 << 10;
+        let region_shapes = if modulus.pow(6) <= MAX_REGION_STATES {
             [(2, 2), (2, 3), (3, 2)]
         } else {
             [(2, 2), (1, 4), (4, 1)]
